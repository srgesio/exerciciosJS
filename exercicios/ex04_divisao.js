function divisao(dividendo, divisor) {
    let divisao = dividendo / divisor
    let resto = dividendo % divisor

    console.log(`Resultado: ${divisao} Resto:${resto}`)
}

divisao(5, 2)
divisao(5, 5)
divisao(30, 4)
divisao(12, 5)